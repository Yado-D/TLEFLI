import 'package:flutter/material.dart';

class partners_login_page extends StatelessWidget {
  const partners_login_page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('partners login'),
      ),
    );
  }
}
